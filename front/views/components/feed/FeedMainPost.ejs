<!-- PARAMETERS: post -->

<article class="bg-[#1d1d1d] rounded-lg mt-[-50px] lg:mt-[-75px] cursor-pointer hover:opacity-75 duration-150" onclick="location.href='/post/<%= post.id %>'">
    <!-- IMAGE -->
    <% if(post.thumbnailURL?.length > 5) { %>
        <img 
        src="<%= post.thumbnailURL %>" alt="Post thumbnail"
        class="w-full h-40 md:h-60 lg:h-64 2xl:h-96 object-cover rounded-lg shadow">
    <% } %>
    <!-- TEXT BOX -->
    <div class="p-4">
        <!-- CATEGORIES -->
        <div class="mt-2">
            <% post.tags.forEach((tag) => { %>
                <span class="text-accent py-1 px-4 bg-[#292929] mr-2 rounded-full text-sm font-semibold">
                    <%= tag %>
                </span>
            <% }); %>
        </div>
        <!-- TITLE AND INFO -->
        <h1 class="text-3xl font-semibold mt-2"><%= post.title %></h1>
        <div class="mt-2 text-sm">
            <span class="opacity-50">
                <% let POST_CREATED_AT = new Date(post.createdAt);  %>
                <%= POST_CREATED_AT.toLocaleDateString() %>
            </span>
            <% if(post.author.name) { %>
                <span class="opacity-50">
                    <span>-</span>
                    <%= post.author.name %>
                </span>
            <% } %>
            <% if(showNewestTag && feed[0].id === post.id) { %>
                <span class="opacity-50 hidden md:inline-block">-</span>
                <span class="bg-accent px-2 rounded-lg hidden md:inline-block font-semibold">MAIS RECENTE</span>
            <% } %>
        </div>
        
    </div>
</article>